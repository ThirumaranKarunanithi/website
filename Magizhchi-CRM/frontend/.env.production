# Production API location — baked into the build by `npm run build`.
# Point this at your deployed backend (Railway). The frontend will call
# https://crm-api.magizhchi.software/api/v1/... in production.
VITE_API_URL=https://crm-api.magizhchi.software
